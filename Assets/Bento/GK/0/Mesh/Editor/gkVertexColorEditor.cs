using UnityEngine;
using UnityEditor;

/*[CustomEditor(typeof(gkVertexColor))]
// The building shape and dimensions attribute editor
public class gkVertexColorEditor : Editor
{
	// The button size
	private static Vector2 mc_f2ButtonSize = new Vector2(100.0f, 30.0f);
	
	// On Inspector GUI
	public override void OnInspectorGUI()
	{
		EditorGUIUtility.LookLikeInspector();
			
		base.OnInspectorGUI();
		
		if(gkGUILayoutUtility.Button("Duplicate Mesh", mc_f2ButtonSize, EGUILayoutOrientation.Horizontal, true))
		{
			gkVertexColor rVertexColor = target as gkVertexColor;
			rVertexColor.Duplicate();
		}
	}
}*/

